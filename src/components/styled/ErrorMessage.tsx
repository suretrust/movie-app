import styled from 'styled-components';

export const ErrorMessage = styled.p`
  color: var(--color-red);
  position: absolute;
  left: 50%;
  transform: translate(-50%, 50%);
  text-align: center;
`;
