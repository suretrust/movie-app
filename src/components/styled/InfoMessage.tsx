import styled from 'styled-components';

export const InfoMessage = styled.p`
  color: var(--color-yellow-omega);
  position: absolute;
  left: 50%;
  transform: translate(-50%, 50%);
  text-align: center;
`;
